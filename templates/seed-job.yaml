apiVersion: batch/v1
kind: Job
metadata:
  name: seed-job
spec:
  template:
    spec:
      containers:
      - name: seed
        image: {{.Values.seed.image }}
        command: ["node", "data.js"]
      restartPolicy: OnFailure
  backoffLimit: 4